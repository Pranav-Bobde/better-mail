{
  "name": "@code-main/db",
  "type": "module",
  "exports": {
    ".": {
      "default": "./src/index.ts"
    },
    "./*": {
      "default": "./src/*.ts"
    }
  },
  "scripts": {
    "check-types": "tsc --noEmit",
    "guard:db": "node scripts/guard-prod-db.mjs",
    "db:push": "node scripts/guard-prod-db.mjs && prisma db push",
    "db:generate": "prisma generate",
    "db:migrate": "node scripts/guard-prod-db.mjs && prisma migrate dev",
    "db:migrate:deploy": "prisma migrate deploy",
    "postinstall": "prisma generate",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@code-main/env": "workspace:*",
    "@neondatabase/serverless": "^1.0.2",
    "@prisma/adapter-neon": "^7.8.0",
    "@prisma/client": "catalog:",
    "dotenv": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@code-main/config": "workspace:*",
    "prisma": "^7.8.0",
    "typescript": "catalog:"
  }
}
